# 2026-04-20 日志

## 09:50 - 免费用户15积分
- 首次访问自动赋予15积分（检查 cine_credits === null）
- 积分系统统一，免费/付费用户共用同一套扣减逻辑
- header badge 对所有用户可见
- 最终完整积分规则：免费=15，7天=100，30天=500，永久=5000
- commit: `feat: 免费用户初始15积分`，已 push

## 09:49 - 调整积分规则
- 30天会员积分从200改为500，同步更新 server.js 和 admin.html
- 最终积分规则：7天=100，30天=500，永久=5000

## 09:44 - AI识别次数改为积分制
- 7天套餐=100积分，30天套餐=200积分，永久=5000积分，每识别1帧消耗1积分
- server.js `/api/verify` 新增返回 `credits` 字段（按有效天数判断套餐）
- index.html：`cine_credits` localStorage存储积分，`checkAIQuota/recordAIUsage/updateAIQuotaBadge` 全部重写
- 首次激活时充值积分（已有积分则不重复充值，防刷）
- header badge 改为「积分 XXX」，积分不足时变红
- admin.html：套餐选项加上积分数量说明
- commit: `feat: 将AI识别次数改为积分制`，已 push

## 09:37 - 修复 Excel 导出无法下载问题
**根本原因**：`buildXlsx` 和 `readZipFilesFromBlob` 存在多处 ZIP 格式 bug：
1. 中央目录签名 `0x01,0x00` 应为 `0x01,0x02`
2. 本地文件头 fnLen/extraLen 字段用了 `setUint32` 应为 `setUint16`
3. EOCD 注释长度字段用了 `setUint32` 应为 `setUint16`
4. `readZipFilesFromBlob` 中央目录字段偏移全错（fnLen 读了 uncompSize 的位置等）
5. DEFLATE 解压应用 `deflate-raw` 而非 `deflate`
- 全部修正，Excel 现在可正常下载
- commit: `fix: 修复Excel导出ZIP格式错误`，已 push

## 09:33 - 移除下载模板按钮
- 删除「下载模板」按钮及 `downloadStoryboardTemplate()` 函数
- 导出 Excel 按钮保留 `videoReady` 检查，未上传素材时提示「请先上传视频」
- commit: `feat: 移除下载模板按钮，导出Excel仅在有素材时可用`，已 push

## 09:30 - Push 到 GitHub
- 将本轮所有修改 push 到 `youzi2020520/videochouzhen` main 分支
- commit: `feat: 灯箱分镜面板可编辑、Excel导出修复、PRO权限调整、下载模板按钮`
- 包含文件：index.html、style.css、storyboard_分镜表.xlsx（新增）
